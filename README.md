electron-skeleton
===================

# Install:

This takes a minute because it has to download the entire electron binary.

```bash
npm install
```

# Run:

```bash
./run.sh
```

# Clean:

```bash
./run.sh clean
```
